name: "NGA publish"
description: "addons publish to nga"
author: "Dencer"
inputs:
  nga-cookie:
    description: "nga cookie"
    required: true
runs:
  using: "node24"
  main: "index.js"
branding:
  color: "blue"
  icon: "play"
